jni混编译的底层实现
perfCounter在类加载中，收集了哪些信息
双亲委派的双亲是什么意思？
不同的类加载器、加载相同的类，但JVM中会作为两个对象，这是真的吗？
    应该是；不过会根据加载器的父子关系定义优先级，越核心越高；（下文的同加载器、不同实例的情况，优先级待确定）
不同的是加载器类、还是加载器实例（比如同个加载器类、实例两次）？
    应该是实例不同，比如tomcat容器对不同上下文的处理不同，只要web.xml中配置的context-param中指定路径不同就不影响；这一点需要验证